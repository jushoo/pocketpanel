{
  "name": "pocketpanel",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "just dev",
    "build": "just build",
    "test": "just test"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
