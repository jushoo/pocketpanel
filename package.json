{
  "name": "pocketpanel",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "make dev",
    "build": "make build",
    "test": "make test"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
